# Pattern Learning Reference

Pattern review uses telemetry and repeated behavior to propose:

- user-model updates
- workflow aliases
- routing defaults
- harness improvements

Require evidence across repeated instances. Never auto-apply. Respect pinned
user-model fields and pinned workflows.

