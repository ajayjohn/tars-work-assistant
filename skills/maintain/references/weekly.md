# Weekly Maintenance Reference

Weekly maintenance produces a review queue, not automatic edits.

Include:

- telemetry rollup
- lint actions
- context gaps
- archive candidates
- user-model and workflow proposals
- Harness review

Harness review covers repeated failures, routing misses, stale aliases,
bloated skill cards, contradictions between docs and implementation, and
workspace state never retrieved after persistence.

