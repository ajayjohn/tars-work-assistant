{
  "$schema": "https://claude.ai/schemas/plugin-mcp-servers.schema.json",
  "_comment": "TARS plugin-shipped MCP servers. tars-vault is the workspace read/write interface; office rendering delegates to first-party document, spreadsheet, and presentation skills.",
  "mcpServers": {
    "tars-vault": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "tars_vault"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/mcp/tars-vault/src",
        "TARS_VAULT_PATH": "${TARS_VAULT_PATH}"
      }
    }
  }
}
