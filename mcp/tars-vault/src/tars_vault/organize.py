"""Organization Engine classifier rules (PRD §3.7). Skeleton."""


def classify(path: str, frontmatter: dict, body: str) -> dict:
    """Return {proposed_path, rationale, confidence}. Placeholder."""
    return {"proposed_path": None, "rationale": "unimplemented", "confidence": 0.0}
