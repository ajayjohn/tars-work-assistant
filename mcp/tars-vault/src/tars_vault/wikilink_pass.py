"""Auto-wikilink pass (PRD §3.3). Skeleton for Phase 1a; impl Phase 2."""


def link_body(body: str, aliases: dict[str, list[str]]) -> dict:
    """Return {body_with_links, ambiguous} — placeholder returns body unchanged."""
    return {"body_with_links": body, "ambiguous": []}
