"""classify_file — Organization Engine classifier — propose target path per taxonomy.

Phase 1a skeleton; rules in Phase 2.
"""
from typing import Any


def classify_file(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'classify_file' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
