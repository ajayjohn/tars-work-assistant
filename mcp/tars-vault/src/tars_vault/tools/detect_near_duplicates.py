"""detect_near_duplicates — Hash + filename + title similarity for near-dup detection.

Phase 1a skeleton.
"""
from typing import Any


def detect_near_duplicates(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'detect_near_duplicates' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
