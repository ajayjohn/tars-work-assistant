"""archive_note — Add tars/archived tag + move to archive/; guardrails per PRD §19.

Phase 1a skeleton.
"""
from typing import Any


def archive_note(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'archive_note' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
