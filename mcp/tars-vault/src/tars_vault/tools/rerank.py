"""rerank — Re-rank a candidate list with recency + tier boosts.

Phase 1a skeleton; impl Phase 4.
"""
from typing import Any


def rerank(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'rerank' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
