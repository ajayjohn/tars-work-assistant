"""semantic_search — Local semantic search (FastEmbed + sqlite-vec).

Phase 1a skeleton; impl Phase 4.
"""
from typing import Any


def semantic_search(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'semantic_search' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
