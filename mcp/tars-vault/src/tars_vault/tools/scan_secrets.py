"""scan_secrets — Wrap scripts/scan-secrets.py; return JSON classifications.

Phase 1a skeleton.
"""
from typing import Any


def scan_secrets(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'scan_secrets' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
