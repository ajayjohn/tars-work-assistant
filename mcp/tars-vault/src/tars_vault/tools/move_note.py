"""move_note — Move a note preserving its wikilink graph.

Phase 1a skeleton; wikilink preservation Phase 2.
"""
from typing import Any


def move_note(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'move_note' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
