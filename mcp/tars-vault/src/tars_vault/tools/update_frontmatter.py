"""update_frontmatter — Wrap obsidian property:set with tars- prefix + enum validation.

Phase 1a skeleton.
"""
from typing import Any


def update_frontmatter(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'update_frontmatter' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
