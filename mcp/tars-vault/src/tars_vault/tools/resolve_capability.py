"""resolve_capability — Resolve a capability to (server, tool) from the integration registry.

Phase 1b skeleton; resolver lands Phase 2.
"""
from typing import Any


def resolve_capability(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'resolve_capability' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
