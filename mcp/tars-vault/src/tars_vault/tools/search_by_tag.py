"""search_by_tag — Wrap obsidian search; return JSON with tag normalisation.

Phase 1a skeleton.
"""
from typing import Any


def search_by_tag(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'search_by_tag' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
