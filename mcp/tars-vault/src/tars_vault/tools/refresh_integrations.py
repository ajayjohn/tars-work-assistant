"""refresh_integrations — Re-run SessionStart discovery and rewrite _system/tools-registry.yaml.

Phase 1b skeleton; discovery lands Phase 2.
"""
from typing import Any


def refresh_integrations(**kwargs: Any) -> dict:
    """Skeleton entry point. Returns a sentinel until later phases wire it up."""
    raise NotImplementedError(
        "tars-vault tool 'refresh_integrations' is a Phase 1a skeleton. "
        "See PRD §26.3 for the full layout and rollout."
    )
