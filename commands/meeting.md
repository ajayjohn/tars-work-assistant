---
description: Process meeting transcript to extract report, tasks, and memory
argument-hint: "[paste transcript or provide file path]"
---

# /meeting

## Protocol
Before following the skill, run the TARS extension pre-flight for `skill="meeting"` using the transcript/source intent: `list_extensions` → `resolve_extension` → `read_extension` for matches → resolve declared capabilities.

Read and follow `skills/meeting/`
