---
description: Draft stakeholder communications with empathy audit and RASCI enforcement
argument-hint: "[recipient and topic]"
---

# /communicate

## Protocol
Before following the skill, run the TARS extension pre-flight for `skill="communicate"` using the recipient/topic as intent: `list_extensions` → `resolve_extension` → `read_extension` for matches → resolve declared capabilities.

Read and follow `skills/communicate/`
