---
description: Generate daily or weekly briefing with calendar, tasks, and people context
argument-hint: "[daily | weekly]"
---

# /briefing

## Protocol
Before following the skill, run the TARS extension pre-flight for `skill="briefing"` using the requested briefing mode and user intent: `list_extensions` → `resolve_extension` → `read_extension` for matches → resolve declared capabilities.

Read and follow `skills/briefing/`

Default: daily mode. Use "weekly" argument or say "weekly briefing" for weekly mode.
