---
description: Generate presentation-grade decks, narratives, and speeches
argument-hint: "[topic and format]"
---

# /create

## Protocol
Before following the skill, run the TARS extension pre-flight for `skill="create"` using the requested artifact/topic as intent: `list_extensions` → `resolve_extension` → `read_extension` for matches → resolve declared capabilities.

Read and follow `skills/create/`
