---
description: Open Collider ideation — generate non-obvious ideas by colliding vault context with distant knowledge domains (power-user shortcut; most users reach this via /think discover)
argument-hint: "[quick | deep | deepen <slug> | review <slug>] [topic or question]"
---

# /ideate

## Protocol
Before following the skill, run the TARS extension pre-flight for `skill="ideate"` using the selected mode and topic as intent: `list_extensions` → `resolve_extension` → `read_extension` for matches → resolve declared capabilities.

Read and follow `skills/ideate/`

Modes: quick (5 min, top 5–7 ideas), deep (15 min, top 10–15 ideas), deepen (iterate from loved ideas in a prior session), review (show past ideas for a project).
Signal-based: "quick" → quick mode, "deep" → deep mode, "deepen [slug]" → deepen mode, "review [slug]" → review mode. Default is quick.
Note: most executives reach ideation naturally through `/think discover` → escalation. This command is a direct shortcut for returning users who know the workflow.
