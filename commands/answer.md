---
description: Fast factual lookup using full search hierarchy
argument-hint: "[question]"
---

# /answer

## Protocol
Before following the skill, run the TARS extension pre-flight for `skill="answer"` using the user's question as intent: `list_extensions` → `resolve_extension` → `read_extension` for matches → resolve declared capabilities.

Read and follow `skills/answer/`
